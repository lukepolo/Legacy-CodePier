@component('mail::message')

Hi, you tried to reset a password which uses an OAuth provider (GitHub / Bitbucket / GitLab). However you cannot
reset a password as you should login with the provider you have selected.

@endcomponent
